## [{{version}}](https://github.com/{{repoUrl}}/compare/{{previousTag}}...{{currentTag}}) ({{date}})

{{#each commits}}
  {{#with (firstLine this.message)}}
    - **{{capitalize this.scope}}:** {{this.subject}} {{#if ../issueUrlFormat}}([#{{../issueNumber this.message ../issuePrefixes}}]({{../issueUrl this.message ../issuePrefixes ../repoUrl}})){{/if}}
    {{#if this.notes}}
      {{#each this.notes}}
        > {{this}}
      {{/each}}
    {{/if}}
  {{/with}}
{{/each}}

Folder name: {{folderName}}
